// localStorageKeys.ts
export const LOCAL_STORAGE_KEYS = {
    BASKET: "basket",
    // You can add more keys here in the future
  } as const; // 'as const' ensures the keys are treated as literal types
  